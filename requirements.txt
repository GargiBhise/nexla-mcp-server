# MCP framework
fastmcp==2.3.3

# PDF parsing (text + tables)
pdfplumber==0.11.4

# Embeddings (local, no API key needed)
sentence-transformers==3.4.1

# Vector store
faiss-cpu==1.9.0

# LLM for answer generation (Ollama runs locally, no API key needed)
requests==2.32.3
